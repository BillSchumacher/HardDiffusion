import 'package:flutter/material.dart';

const inputPadding =
    EdgeInsets.only(top: 4.0, bottom: 4.0, left: 2.0, right: 2.0);
const inputDecoration = InputDecoration(
  border: OutlineInputBorder(),
  isDense: true,
  filled: true,
  fillColor: Colors.white,
  hintText: '',
);
